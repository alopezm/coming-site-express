<!doctype html>
<html lang="{{metadata/lenguaje}}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{metadata/title}}</title>
    {{#if metadata/description}}<meta name="description" content="{{metadata/description}}">{{/if}}
    {{#if metadata/author}}<meta name="author" content="{{metadata/author}}">{{/if}}
    {{#if metadata/keywords}}<meta name="keywords" content="{{metadata/keywords}}">{{/if}}
    <style>
      {{styles}}
    </style>
  </head>
  <body>
    {{>slideData}}
    {{>webFontLoader}}
  </body>
</html>
